all: $(SHLIB) 

PKG_CXXFLAGS = -Iinclude ${SHLIB_OPENMP_CXXFLAGS}
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS) ${SHLIB_OPENMP_CXXFLAGS}

%.o: %.cpp
	$(CXX) $(ALL_CPPFLAGS) $(ALL_CXXFLAGS) -Wall -pedantic -c $< -o $@ 

